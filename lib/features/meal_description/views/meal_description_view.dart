import 'package:flutter/material.dart';

class MealDescriptionView extends StatelessWidget {
  const MealDescriptionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
